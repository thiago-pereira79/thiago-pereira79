name: Atualizar projetos em destaque

on:
  schedule:
    - cron: "0 12 * * *"   # roda 1x por dia às 12:00 UTC (~09:00 BRT)
  push:
    branches: [ main, master ]
  workflow_dispatch:       # permite rodar manualmente no botão "Run workflow"

permissions:
  contents: write   # garante permissão para atualizar o README automaticamente

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Gerar lista de repositórios (top 3 recentes)
        uses: actions/github-script@v7
        id: gen
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const fs = require('fs');

            // 1) Buscar repositórios públicos do usuário
